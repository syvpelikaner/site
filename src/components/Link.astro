---
export interface Props {
  href: string;
  class?: string;
}
const { href, class: classNames = "" } = Astro.props;
---

<a href={href} class={"link link-hover font-cinzel " + classNames}><slot /></a>
